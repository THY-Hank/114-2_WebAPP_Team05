# Team5
這是Team5的weekly HW專案 
打算做一個具有聊天室的程式碼管理網站，像是部分github功能+即時聊天室
## push your code by the following command
```
git add .
git commt -m "commit message"
git checkout -b branchname
git push origin branchname
```
## you can use this to check your html code
```
npx htmlhint "**/*.html"
```
